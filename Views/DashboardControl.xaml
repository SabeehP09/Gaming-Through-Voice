<UserControl x:Class="GamingThroughVoiceRecognitionSystem.Views.DashboardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:GamingThroughVoiceRecognitionSystem.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1320">

    <UserControl.Resources>
        <!-- Value Converters -->
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
        <converters:NotNullToVisibilityConverter x:Key="NotNullToVisibilityConverter"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>

        <!-- Gaming Card Style with Smart Shadow -->
        <Style x:Key="HoverableCardStyle" TargetType="Border">
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="12" ShadowDepth="2" Opacity="0.1"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)" To="1.05" Duration="0:0:0.2">
                                    <DoubleAnimation.EasingFunction>
                                        <CubicEase EasingMode="EaseOut"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)" To="1.05" Duration="0:0:0.2">
                                    <DoubleAnimation.EasingFunction>
                                        <CubicEase EasingMode="EaseOut"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                                <DoubleAnimation Storyboard.TargetProperty="Effect.(DropShadowEffect.BlurRadius)" To="20" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="Effect.(DropShadowEffect.Opacity)" To="0.15" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="Effect.(DropShadowEffect.BlurRadius)" To="12" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="Effect.(DropShadowEffect.Opacity)" To="0.1" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Gaming Action Button -->
        <Style x:Key="GlassyButton" TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="Background">
                <Setter.Value>
                    <Binding Source="{StaticResource PrimaryGradientBrush}"/>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderBrush" Value="{DynamicResource GlassBorderBrush}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8">
                            <Border.Effect>
                                <DropShadowEffect x:Name="btnGlow" Color="{StaticResource PrimaryGlowColor}" BlurRadius="10" ShadowDepth="0" Opacity="0.5"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="btnGlow" Storyboard.TargetProperty="BlurRadius" To="20" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="btnGlow" Storyboard.TargetProperty="Opacity" To="0.9" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="btnGlow" Storyboard.TargetProperty="BlurRadius" To="10" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="btnGlow" Storyboard.TargetProperty="Opacity" To="0.5" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer Padding="32" VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <!-- Gaming Welcome Header -->
            <TextBlock x:Name="WelcomeText" Text="WELCOME BACK, GAMER! 🎮" FontSize="38" FontWeight="Black" 
                       Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,8">
                <TextBlock.Effect>
                    <DropShadowEffect Color="{StaticResource PrimaryGlowColor}" BlurRadius="15" ShadowDepth="0" Opacity="0.6"/>
                </TextBlock.Effect>
            </TextBlock>
            <TextBlock Text="⚡ Ready to dominate with your voice? Choose your game and let's go!" 
                       FontSize="16" Foreground="{DynamicResource TextSecondaryBrush}" 
                       FontWeight="SemiBold" Margin="0,0,0,32"/>

            <!-- Stats Cards -->
            <UniformGrid Columns="3" Margin="0,0,0,32">
                <Border Style="{StaticResource HoverableCardStyle}" Margin="0,0,12,0" Background="{DynamicResource GlassBackgroundBrush}" BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1" CornerRadius="20" Padding="20">
                    <StackPanel>
                        <TextBlock x:Name="GamesPlayedCard" Text="0" FontSize="28" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Text="Games Played" FontSize="14" Foreground="{DynamicResource TextSecondaryBrush}"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource HoverableCardStyle}" Margin="6,0,6,0" Background="{DynamicResource GlassBackgroundBrush}" BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1" CornerRadius="20" Padding="20">
                    <StackPanel>
                        <TextBlock x:Name="VoiceCommandsCard" Text="0" FontSize="28" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Text="Authentications" FontSize="14" Foreground="{DynamicResource TextSecondaryBrush}"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource HoverableCardStyle}" Margin="12,0,0,0" Background="{DynamicResource GlassBackgroundBrush}" BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1" CornerRadius="20" Padding="20">
                    <StackPanel>
                        <TextBlock x:Name="HoursPlayedCard" Text="0" FontSize="28" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Text="Hours Played" FontSize="14" Foreground="{DynamicResource TextSecondaryBrush}"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- Games Section -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="My Games" FontSize="24" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,24"/>
                <Button Content="+ Add Game" Style="{StaticResource GlassyButton}" Height="40" Padding="20,8" Click="AddGame_Click" Grid.ColumnSpan="2" Margin="1138,8,0,8"/>
            </Grid>

            <ItemsControl x:Name="GamesGrid">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource HoverableCardStyle}" 
                                Width="320" 
                                Height="380" 
                                Margin="0,0,20,20" 
                                Background="{DynamicResource GlassBackgroundBrush}" 
                                BorderBrush="{DynamicResource GlassBorderBrush}" 
                                BorderThickness="2" 
                                CornerRadius="20" 
                                Padding="20">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Game Icon -->
                                <Border Grid.Row="0" 
                                        Width="100" 
                                        Height="100" 
                                        Background="{DynamicResource SidebarBackgroundBrush}" 
                                        BorderBrush="{DynamicResource GlassBorderBrush}" 
                                        BorderThickness="2" 
                                        CornerRadius="16" 
                                        Margin="0,0,0,16"
                                        HorizontalAlignment="Center">
                                    <Border.Effect>
                                        <DropShadowEffect Color="{StaticResource PrimaryGlowColor}" BlurRadius="12" ShadowDepth="0" Opacity="0.3"/>
                                    </Border.Effect>
                                    <Grid>
                                        <TextBlock Text="🎮" 
                                                   FontSize="48" 
                                                   HorizontalAlignment="Center" 
                                                   VerticalAlignment="Center"
                                                   Visibility="{Binding IconPath, Converter={StaticResource NullToVisibilityConverter}}"/>
                                        <Image Source="{Binding IconPath}" 
                                               Stretch="Uniform" 
                                               Margin="8"
                                               Visibility="{Binding IconPath, Converter={StaticResource NotNullToVisibilityConverter}}"/>
                                    </Grid>
                                </Border>

                                <!-- Game Info -->
                                <StackPanel Grid.Row="1">
                                    <TextBlock Text="{Binding GameName}" 
                                               FontSize="18" 
                                               FontWeight="Bold" 
                                               Foreground="{DynamicResource TextPrimaryBrush}" 
                                               TextAlignment="Center"
                                               TextTrimming="CharacterEllipsis"
                                               Margin="0,0,0,12"/>

                                    <TextBlock Text="Key Controls:" 
                                               FontSize="12" 
                                               FontWeight="SemiBold" 
                                               Foreground="{DynamicResource TextSecondaryBrush}" 
                                               Margin="0,0,0,8"/>

                                    <ScrollViewer MaxHeight="120" VerticalScrollBarVisibility="Auto">
                                        <ItemsControl ItemsSource="{Binding Controls}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Background="{DynamicResource SidebarBackgroundBrush}" 
                                                            BorderBrush="{DynamicResource GlassBorderBrush}" 
                                                            BorderThickness="1" 
                                                            CornerRadius="8" 
                                                            Padding="8" 
                                                            Margin="0,0,0,6">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>
                                                            <StackPanel Grid.Column="0">
                                                                <TextBlock Text="{Binding ActionName}" 
                                                                           FontSize="11" 
                                                                           FontWeight="SemiBold" 
                                                                           Foreground="{DynamicResource TextPrimaryBrush}"/>
                                                            </StackPanel>
                                                            <TextBlock Grid.Column="1" 
                                                                       Text="{Binding KeyBinding}" 
                                                                       FontSize="10" 
                                                                       FontWeight="Bold" 
                                                                       Foreground="{DynamicResource TextPrimaryBrush}" 
                                                                       VerticalAlignment="Center"
                                                                       Margin="8,0,0,0"/>
                                                        </Grid>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ScrollViewer>
                                </StackPanel>

                                <!-- Action Buttons -->
                                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,0">
                                    <Button Content="▶ Play" 
                                            Style="{StaticResource GlassyButton}" 
                                            Width="100" 
                                            Height="36" 
                                            Margin="0,0,8,0"
                                            Tag="{Binding}"
                                            Click="PlayGame_Click"/>
                                    <Button Content="✏️ Edit" 
                                            Width="36" 
                                            Height="36" 
                                            Background="Transparent" 
                                            BorderBrush="{DynamicResource GlassBorderBrush}" 
                                            BorderThickness="2" 
                                            Foreground="{DynamicResource TextPrimaryBrush}" 
                                            FontSize="14" 
                                            Cursor="Hand"
                                            Margin="0,0,8,0"
                                            Tag="{Binding}"
                                            Click="EditGame_Click"
                                            Visibility="{Binding IsDefault, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                                    <Button Content="🗑️" 
                                            Width="36" 
                                            Height="36" 
                                            Background="Transparent" 
                                            BorderBrush="#EF4444" 
                                            BorderThickness="2" 
                                            Foreground="#EF4444" 
                                            FontSize="14" 
                                            Cursor="Hand"
                                            Tag="{Binding}"
                                            Click="DeleteGame_Click"
                                            Visibility="{Binding IsDefault, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
